//? enum for better readability 
enum AppScreen {
  explore, //ProjectsScreen
  architectures,
  models,
  datasets,
  //projects, //  ProjectsScreen
  learn,
  docs, // Corresponds to  "Docs" sidebar item
  settings, // Corresponds to  SettingsScreen
  profile, // For the ProfileWidget's screen
}
