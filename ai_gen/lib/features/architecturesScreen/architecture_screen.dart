import 'package:flutter/material.dart';

class ArchitecturesScreen extends StatelessWidget {
  const ArchitecturesScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text('Architectures Screen Content',
            style: TextStyle(fontSize: 30)));
  }
}
